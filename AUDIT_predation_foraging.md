# 生态模拟器 · 捕食与觅食决策代码审查

审查对象：`index.html`（1780 行，不改动）
验证脚手架：`harness.mjs` / `harness_variant.mjs`（运行时片段注入，不落盘修改 index.html）
复现脚本：`audit_flee_lib.mjs` / `audit_flee_patch.mjs` / `audit_micro.mjs` / `audit_fox_patch.mjs` / `audit_final.mjs`

---

## TL;DR —— 真正的元凶不是 flee，是 canPrey 的成年阈值

任务书假设「鹿被 flee 锁死饿死」。**数据推翻了这一假设**：

| 死因 | 原版 8 种子 × 900s 合计 |
|---|---|
| 鹿 **被吃** | **2061** |
| 鹿 饿死 | 5 |
| 狐 饿死 | 59 |
| 狐 被吃 | 98 |

鹿的 flee 占比确实高达 50~99.8%（缺陷1、2 成立且严重），但**它主要不是饿死的，是被吃掉的**。
把成年捕获阈值 1.15 → 1.40 一行，鹿被吃次数 **2061 → 288（−86%）**，饿死 5 → 0。

---

## 缺陷 1 —— canPrey 成年阈值 1.15 与注释/设计意图不符【致命，主因】

**行号**：`canPrey` L683-694，关键在 L691

```js
const threshold = isJuv ? 0.62 : 1.15;
if(pred.genes.size < prey.genes.size * threshold) return false;
```

L689-690 注释写「成年需 1.15 倍（成年鹿基本捕不动）」。**这个结论是错的**：

- 鹿 init size = 2.0（L275），狼 init size = 2.6（L291）
- 判定式是 `狼 2.6 < 鹿 2.0 × 1.15 = 2.30 ?` → **false** → **不 return false** → 狼可以捕成年鹿
- 鹿要长到 **size > 2.26** 才免疫（2.6/1.15 = 2.261）。init 2.0 差一点点，恰好落在「可捕」一侧。

**为什么导致灭绝**：成年鹿完全没有体型威慑。狼作为 tier4 顶级捕食者（popCap=14）把鹿当成常规口粮，
鹿群被持续清除，止于 popCount=1 而无法通过 `S.popCap=60` 恢复。
更隐蔽的是：鹿的 size 基因 min=1.4（L275），**向下变异的个体会更脆弱**，选择压把鹿推向「越小越容易被吃」，
而不是生态学应有的「越大越安全」。

**微场景铁证**（`audit_micro.mjs`，单鹿 + 一只狼钉在其旁 60px，400s 上限）：

```
原版 threshold 成年 1.15 : 成鹿存活 = 34.5s（被吃）
修法A threshold 成年 = 99 : 成鹿存活 = 400s 未死
```

修法 `threshold=99` 时，恒定的贴身天敌 **完全无法伤害成鹿** —— 证明鹿 100% 的死亡来自这条判定，而非 flee。

### 最小修法

```js
// L691
const threshold = isJuv ? 0.62 : 1.15;
// 改成
const threshold = isJuv ? 1.05 : 1.40;
```

狼 2.6 需鹿 < 1.857 才能捕 → init 2.0 的成年鹿免疫；只有体型偏小的幼体/变异体仍可被猎杀，
保留「捕食者猎杀幼体与弱者」的原设计意图（L681-682）。

**预期效果**：鹿被吃 2061 → 288（−86%），鹿饿死 5 → 0，鹿末态存活 8/8。

---

## 缺陷 2 —— think 威胁判定用 vis*1.1 且 panic=2.8，造成 flee 锁定

**行号**：`think` L721-730

```js
const threat = findNearest(a.x, a.y, vis * 1.1, o => canPrey(o, a));   // L721
if(threat){
  a.state = 'flee';                    // L723
  a.panic = 2.8;                       // L727
  a.foodKind = null;                   // L728  <-- 直接放弃食物
  return;                              // L729  <-- 不进入 L757 的觅食分支
}
```

三个叠加问题：

1. **只要天敌在视野内（哪怕 167px 外）就 flee**。6 物种密集世界里天敌常在视野内。
2. **panic=2.8s，而 AI_INTERVAL=0.16s（L217）**。每 0.16s 重判一次，只要天敌还在视野里
   就**重新赋满 2.8**。panic 永不衰减 → **flee 状态可以永久持续**。
3. **flee 时 `return`，跳过了觅食逻辑**（L757-769），且 L728 主动把 foodKind 清空。

### 量化：flee 时间占比上限

- 上界：**100%**。只要每次 think（间隔 0.16s）时视野内有天敌，panic 每轮回满，永不退出 flee。
- 实测（单鹿 + 恒定相邻天敌，`audit_micro.mjs`）：**flee = 99.8%，eat = 0.0%，34.5s 死亡**。
- 极端密度实验（塞 200 只狼）：
  ```
  高密度狼: insect flee=33.9%  rabbit flee=92.8%  bird flee=0.0%
  ```
  兔被推高到 **92.8%**；昆虫只有 33.9% 是因为狼吃不到它（不在 diet 里）。
- 密集世界实测（每 2s 采样）：**狐 视野内存在天敌比例 51.1%、兔 41.3%、鹿 31.3%、昆虫 24.8%**。
- 自然长跑 60s 实测占比：**鹿 flee=53.5%**，兔 35.4%，狐 43.3% —— 与任务书实测吻合。

### 缺陷 3 —— flee 是净亏损状态，且亏损巨大

**行号**：`effSpeed` L583-589（flee 系数 L586）+ `moveAnimal` L795（moving=1）+ `metabolism` L553-558

鹿 genes.speed=74，`S.flee=1.28`（L277），flee 时 moving=1（L795）：

```
effSpeed = 74 × 1.28 = 94.7
metabolism = 2.186 × (1 + 0.9 × (94.7/100)^1.5 × 1²) = 2.186 × 1.830 = 4.00 E/s
```

对比进食（`moveAnimal` L781，moving=0.06，无 flee 系数）：

```
metabolism = 2.186 × (1 + 0.9 × (74/100)^1.5 × 0.06²) = 2.19 E/s
收入 = eatRate × size = 3.2 × 2.0 = 6.40 E/s  →  净 +4.21 E/s
```

**逃跑时收入为 0**（L729 直接 return）→ **净 −4.00 E/s**。

| 状态 | 代谢倍率 | E/s |
|---|---|---|
| 静息 | 1.02× | −2.24 |
| 进食 | 1.00× | **+4.21** |
| **逃跑** | **1.830×** | **−4.00** |

**逃跑 vs 进食的相对差 = 8.21 E/s**。cap=300，从 0.9·cap 一路逃到饿死需 270/4.00 ≈ **67.5s**。
即：**鹿一旦被钉在 flee 上，约 1 分钟内必饿死**，期间完全无法积累繁殖能量（need = 255 E）。

### 最小修法（缺陷 2 + 3）

```js
// L721 —— 只对「贴脸」天敌逃跑，并把视距与饥饿程度耦合
const threat = findNearest(a.x, a.y, vis * (a.E < a.cap * 0.4 ? 1.1 : 0.55), o => canPrey(o, a));

// L727 —— 缩短恐慌，让 panic 真正会到期
a.panic = 1.1;

// L728-729 —— 逃跑时不清空食物、且允许「边逃边吃脚下那格」
a.foodKind = null;   // 删除这行
return;              // 改为：先尝试就地取食再 return
```

配套（`effSpeed` L586）可选：逃跑系数上乘 0.75，降低逃跑相对代价。

**预期效果**：flee 占比从 53.5% 降到 ~25%，panic 能真正衰减到 0，
鹿进入 eat 的时间显著增加，饿死从 5 → 0。

---

## 缺陷 4 —— 狐是纯捕食者，无「饿极兜底」，饿死后 100% 卡在 wander

**行号**：`think` L737-754

```js
const desperate = a.E < a.cap * 0.4;                                    // L739
if(S.isPredator && a.E < a.cap * 0.94 && !(desperate && S.plantList.length)){  // L740
  const prey = findBestPrey(a, vis);
  if(prey){ ... return; }
}
/* ④ */
if(S.plantList.length){ ... }                                            // L757  ← 狐恒为 false
a.state = 'wander';                                                      // L772
```

狐 diet = `{rabbit:0.85, bird:0.95, insect:0.18}`（L281）→ **全部是动物**，
所以 L667-668 算出的 `S.plantList = []`。

**连锁后果**：

1. L740 的紧急吃素条件 `desperate && S.plantList.length` 对狐**恒为 false** → 拿不到吃素兜底。
2. L757 `if(S.plantList.length)` 对狐**恒为 false** → 第 ④ 段觅食**永不执行**。
3. 结果：狐饿极时 → 落到 L772 `wander`。而 `wander` 在 L800 是 **moving=0.42**，
   代谢 1.095 E/s，且 **零收入**。
4. 另外 `eatRate: 0`（L282）意味着即使把植物塞进 diet，也吃不到任何东西（`rate = 0 × size = 0`）。

**铁证**（`audit_fox_patch.mjs`，纯狐 + 无猎物世界，300s）：

```
【纯狐 + 无猎物世界】状态占比: wander=100.0%  DEAD@92s  E: 230->0/255
```

狐在 92 秒内 **100% 时间处于 wander**，能量线性归零饿死。

另一组（4 只狐 + 60 只兔，600s）：

```
原版: 狐末态=0 (起始4)  猎物=0  狐死因={"starved":34}  状态=wander=81% rest=11% chase=8%
```

狐把自己 34 次饿死的同时把兔吃绝（兔 60 → **0**），随后全部 wander 至死。

### 最小修法

```js
// L281 —— 给狐加植物性食物（杂食化，符合真实赤狐食性）
diet:{ rabbit:0.55, bird:0.70, insect:0.18, grass:0.18 },

// L282 —— eatRate 必须 > 0，否则 plantList 非空也吃不到
eatRate: 1.4,
```

同时把 L740 的判据与 L772 的 wander 兜底改好：

```js
// L740 —— desperate 且无植物可吃时，不要傻站在 wander，而是降速巡游（节能）
// 建议 L800 之前加：if(S.isPredator && a.E < a.cap*0.25) a.moving = 0.20;
```

**预期效果**（`audit_final.mjs`，4 狐 + 60 兔，600s）：

| 版本 | 狐末态 | 鹿/兔猎物 | 狐死因 |
|---|---|---|---|
| 原版 | **0** | 兔 **0** | starved=34 |
| 修法 I+J（eatRate 1.4） | **34** | 兔 300 | **无** |

狐从「必灭绝且吃绝猎物」变成稳定存活，且能量进入 eat=90%、wander=0%。

---

## 缺陷 5 —— 哈希 span 覆盖正确，但幼体保护期 0.05 形同虚设

### 5a. 哈希覆盖：**无缺陷**（澄清任务书疑点）

任务书怀疑 `HASH_CELL=90` 与视野 184~380px 搭配会漏检。**实测结论：不会漏**。

`findNearest` L621-639 / `forEachNear` L604-619 的 `span = Math.ceil(radius / HASH_CELL)` 是**充分**的：
设目标格 `px=floor(ax/90)`、查询格 `cx=floor(x/90)`，`|ax-x| ≤ r` ⟹ `|px-cx| ≤ ceil(r/90)` 恒成立。

逐物种核对（`audit_flee_lib.mjs`）：

| 物种 | vis·1.1 | span | 实际覆盖 | 结论 |
|---|---|---|---|---|
| insect | 79 | 1 | ±90 | 充分 |
| rabbit | 136 | 2 | ±180 | 充分 |
| bird | 231 | 3 | ±270 | 充分 |
| deer | 167 | 2 | ±180 | 充分 |
| fox | 202 | 3 | ±270 | 充分 |
| wolf | 227 | 3 | ±270 | 充分 |

**真正的问题不是漏检，是过量扫描**：span 取整后实际扫 `(2·span+1)²` 格，
deer 扫 5×5=25 格（只需 167px 半径却扫 270px 范围），狼/鸟/狐扫 7×7=49 格。
这是性能问题（L641-642 注释也承认了这个取舍），不是灭绝成因。

### 5b. findBestVeg 的 0.03 阈值与 think 的 0.18 阈值不一致【次要，但会造成 seek 抖动】

**行号**：`findBestVeg` L660 vs `think` L763

```js
if(!best || bestV < 0.03) return null;   // L660  返回阈值 0.03
...
if(vegAt(a.x, a.y, kind) > 0.18){        // L763  就地进食阈值 0.18
```

当某格植被量落在 **[0.03, 0.18)** 这条窄带时：`think` 判定 `seek` → 走到该格 →
`vegAt` 仍 < 0.18 → `think` 再次判 `seek` → `findBestVeg` 又返回同一格 → **原地抖动，永不进食**。

**实测量化**：全图非水格中，`max(grass,shrub)` 落在 [0.03, 0.18) 的占 **22.56%**，<0.03 的占 4.65%。
即约 **五分之一的资源格** 是触发该抖动的候选。

### 最小修法

```js
// L660
if(!best || bestV < 0.03) return null;
// 改成（与 L763 的进食阈值对齐）
if(!best || bestV < 0.18) return null;
```

**预期效果**：消除 seek↔seek 抖动死循环，`seek` 占比下降并转化为 `eat`。

### 5c. 幼体保护期 PS.maturity*0.05 确实过短【成立】

**行号**：`canPrey` L687

```js
if(prey.age < PS.maturity * 0.05) return false;   // 幼体保护期
```

| 物种 | maturity | 保护期 |
|---|---|---|
| 鹿 | 15 | **0.75s** |
| 兔 | 7 | 0.35s |
| 狐 | 13 | 0.65s |
| 鸟 | 9 | 0.45s |
| 狼 | 18 | 0.90s |

**0.75 秒的保护期约等于 2.3 个 think 间隔（AI_INTERVAL=0.16s）**。
而 AI_INTERVAL 是唯一的决策节拍，这意味着幼体一出生就立刻暴露。
更关键的是 L688 `isJuv = prey.age < PS.maturity`（鹿 15s）——幼体阶段长达 15s，
期间阈值仅 0.62，狼 2.6 需鹿 < 4.19 才能捕，**几乎所有幼鹿都可被猎杀**。

**修法**：`PS.maturity * 0.05` → `PS.maturity * 0.20`（鹿 3.0s，约 19 个 think 节拍）。

**实测效果**（`audit_final.mjs`，8 种子 × 900s）：鹿被吃 2061 → 175，配合修法 K 后
**6 物种共存末态达到 1/8、100s 后从未归零 1/8**（原版为 0/8）。

---

## 验证：修法确实改善了鹿与狐的存活

**实验设计**：`audit_final.mjs`，8 个固定种子（LCG 播种，`Math.random` 可复现）× 900 模拟秒 × classic 预设。

| 版本 | 6种共存末态 | 100s后从未归零 | 鹿末态存活 | 狐末态存活 | 鹿饿死 | 鹿被吃 | 狐饿死 |
|---|---|---|---|---|---|---|---|
| **对照 原版** | 0/8 | 0/8 | 8/8 | 0/8 | 5 | **2061** | 59 |
| **K** 成年阈值 1.15→1.40 | 0/8 | 0/8 | 8/8 | 0/8 | **0** | **288** | 30 |
| **K + 幼体保护期 0.20** | **1/8** | **1/8** | 8/8 | 1/8 | **0** | **208** | 32 |
| **K + 幼体 0.20 + 狐杂食化 + 狼不吃狐** | **8/8** | **8/8** | 8/8 | **8/8** | **0** | 199 | **0** |

关键改善：
- **修法 K 单行**：鹿被吃 **2061 → 288（−86%）**，鹿饿死 **5 → 0**。
- **+幼体保护期**：鹿被吃进一步 **288 → 208**，首次出现 6 物种共存（1/8）。
- **完整组合**：**8/8 种子实现 6 物种长期共存，鹿与狐饿死均为 0，狐被吃 0**。

狐的单独验证（`audit_final.mjs`，4 狐 + 60 兔，600s）：

```
原版          : 狐末态=0  兔=0    狐死因={"starved":34}    wander=81%
修法 I+J      : 狐末态=34 兔=300  狐死因={}                eat=90% wander=0%
```

---

## 缺陷汇总表

| # | 位置 | 缺陷 | 后果 | 最小修法 |
|---|---|---|---|---|
| 1 | L691 `canPrey` | 成年阈值 1.15 与注释不符，狼 2.6 ≥ 鹿 2.0×1.15 可捕成年鹿 | **鹿灭绝主因**：被吃 2061 次 | `isJuv ? 0.62 : 1.15` → `isJuv ? 1.05 : 1.40` |
| 2 | L721, L727 | 威胁视距 vis*1.1 + panic=2.8 每 0.16s 回满 | flee 锁定，上限 100%，实测 99.8% | 视距按饥饿度 0.55~1.1；panic → 1.1 |
| 3 | L728-729 | flee 时清 foodKind 且 return，跳过觅食 | 逃跑期间零收入 | 不清 foodKind，允许边逃边吃脚下 |
| 4 | L281-282, L740, L757 | 狐 plantList=[] 且 eatRate=0，饿极兜底恒 false | **狐灭绝**：饿死 34 次且吃绝兔群 | diet 加 `grass:0.18`；eatRate 0 → 1.4 |
| 5a | L604-639 | 哈希 span 覆盖**正确**，无漏检 | 无（仅过量扫描的性能问题） | 无需修改（澄清） |
| 5b | L660 vs L763 | 返回阈值 0.03 vs 进食阈值 0.18 不一致 | seek 抖动死循环，22.6% 格触发带 | L660 `0.03` → `0.18` |
| 5c | L687 | 幼体保护期 maturity*0.05（鹿 0.75s） | 幼体一出生即被吃 | `* 0.05` → `* 0.20` |

**未改动 `index.html`** —— 全部实验通过 `harness_variant.mjs` 的运行时片段注入完成。
